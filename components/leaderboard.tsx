"use client";
import {useEffect,useState} from 'react';import {Trophy} from 'lucide-react';type Entry={id:string;x_username:string;gg_thesis_url:string};type Score={entryId:string;total:number;evidence:number;invalidation:number;mechanism:number;risk:number;status:string};export function Leaderboard(){const [data,setData]=useState<{scores:Score[];entries:Entry[]}|null>(null);useEffect(()=>{Promise.all([fetch('/api/leaderboard').then(r=>r.json()),fetch('/api/entries').then(r=>r.json())]).then(([board,list])=>{if(board.published)setData({scores:board.scores,entries:list.entries})}).catch(()=>{})},[]);if(!data)return null;return <section className="leaderboard"><header><div><p className="eyebrow">OFFICIAL RESULTS</p><h2>Thesis of the Week leaderboard</h2></div><Trophy/></header><div className="leaderboard-list">{data.scores.map((score,index)=>{const entry=data.entries.find(e=>e.id===score.entryId);if(!entry)return null;return <a className="leader-row" href={entry.gg_thesis_url} target="_blank" rel="noopener noreferrer" key={score.entryId}><strong>#{index+1}</strong><span>@{entry.x_username}</span><small>Evidence {score.evidence}/3 / Invalidation {score.invalidation}/3 / Mechanism {score.mechanism}/2 / Risk {score.risk}/2</small><b>{score.total}/10</b></a>})}</div></section>}
